(function(f){if(typeof exports==="object"&&typeof module!=="undefined"){module.exports=f()}else if(typeof define==="function"&&define.amd){define([],f)}else{var g;if(typeof window!=="undefined"){g=window}else if(typeof global!=="undefined"){g=global}else if(typeof self!=="undefined"){g=self}else{g=this}g.jiboPlugins = f()}})(function(){var define,module,exports;return (function e(t,n,r){function s(o,u){if(!n[o]){if(!t[o]){var a=typeof require=="function"&&require;if(!u&&a)return a(o,!0);if(i)return i(o,!0);var f=new Error("Cannot find module '"+o+"'");throw f.code="MODULE_NOT_FOUND",f}var l=n[o]={exports:{}};t[o][0].call(l.exports,function(e){var n=t[o][1][e];return s(n?n:e)},l,l.exports,e,t,n,r)}return n[o].exports}var i=typeof require=="function"&&require;for(var o=0;o<r.length;o++)s(r[o]);return s})({1:[function(require,module,exports){
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var path = require("path");
var Module = require("module");
var findRoot = require("find-root");
var PathUtils = (function () {
    function PathUtils() {
    }
    PathUtils.findRoot = function (start) {
        if (start) {
            var assetPack = PathUtils.getAssetPack(start);
            if (assetPack) {
                return PathUtils.resolveAssetPack(assetPack);
            }
        }
        return findRoot(start || process.cwd());
    };
    PathUtils.getProjectName = function (dir) {
        return require(path.join(dir, 'package.json')).name;
    };
    PathUtils.isURL = function (uri) {
        return uri.startsWith('https://') || uri.startsWith('http://') || uri.startsWith('file://');
    };
    PathUtils.isImage = function (uri) {
        return uri && /^\.(jpg|png|gif|jpeg)$/i.test(path.extname(uri));
    };
    PathUtils.setDefaultPath = function (parentDir, fileName) {
        var split = fileName.split(PathUtils.ASSET_TOKEN);
        if (split.length === 2) {
            return split[0] + PathUtils.ASSET_TOKEN + path.join(parentDir, split[1]);
        }
        else {
            return path.join(parentDir, fileName);
        }
    };
    PathUtils.getAssetPack = function (fileName) {
        var split = fileName.split(PathUtils.ASSET_TOKEN);
        return (split.length === 1) ? "" : split[0];
    };
    PathUtils.resolveAssetPack = function (assetPack) {
        if (assetPack && assetPack.endsWith(PathUtils.ASSET_TOKEN)) {
            assetPack = assetPack.substr(0, assetPack.length - PathUtils.ASSET_TOKEN.length);
        }
        if (assetPack === 'core' || assetPack === 'jibo') {
            assetPack = 'jibo';
            var packagePath_1 = PathUtils.resolve(assetPack + "/package.json");
            if (!packagePath_1) {
                return PathUtils.getPackagePath();
            }
            return path.dirname(packagePath_1);
        }
        var packagePath = PathUtils.resolve(assetPack + "/package.json");
        return path.dirname(packagePath);
    };
    PathUtils.getAssetUri = function (fileName, assetPack, resourceRoot) {
        if (assetPack === void 0) { assetPack = ''; }
        var split = fileName.split(PathUtils.ASSET_TOKEN);
        if (split.length === 2) {
            assetPack = split[0];
            fileName = split[1];
        }
        if (path.isAbsolute(fileName)) {
            return fileName;
        }
        else if (assetPack) {
            resourceRoot = PathUtils.resolveAssetPack(assetPack);
        }
        else if (!resourceRoot) {
            resourceRoot = PathUtils.findRoot();
        }
        return path.join(resourceRoot, fileName);
    };
    PathUtils.getAudioUri = function (fileName, currentAssetPack, resourceRoot) {
        if (currentAssetPack === void 0) { currentAssetPack = ''; }
        var audioPath = PathUtils.setDefaultPath('audio', fileName);
        return PathUtils.getAssetUri(audioPath, currentAssetPack, resourceRoot);
    };
    PathUtils.getTimelineUri = function (fileName, currentAssetPack, resourceRoot) {
        if (currentAssetPack === void 0) { currentAssetPack = ''; }
        var timelinePath = PathUtils.setDefaultPath('timelines', fileName);
        return PathUtils.getAssetUri(timelinePath, currentAssetPack, resourceRoot);
    };
    PathUtils.getAnimationUri = function (fileName, currentAssetPack, resourceRoot) {
        if (currentAssetPack === void 0) { currentAssetPack = ''; }
        var animPath = PathUtils.setDefaultPath('animations', fileName);
        return PathUtils.getAssetUri(animPath, currentAssetPack, resourceRoot);
    };
    PathUtils.resolve = function (moduleId, fromPath) {
        var fromDir = path.resolve(fromPath || PathUtils.getPackagePath());
        var fromFile = path.join(fromDir, 'noop.js');
        try {
            var filepath = Module._resolveFilename(moduleId, {
                id: fromFile,
                filename: fromFile,
                paths: Module._nodeModulePaths(fromDir)
            });
            return filepath;
        }
        catch (err) {
            return null;
        }
    };
    PathUtils.getPackagePath = function () {
        var uri = process.cwd();
        if (global.atom) {
            uri = atom.project.getPaths()[0];
        }
        var root = findRoot(uri);
        return root;
    };
    return PathUtils;
}());
PathUtils.ASSET_TOKEN = '://';
exports.default = PathUtils;

},{"find-root":undefined,"module":undefined,"path":undefined}],2:[function(require,module,exports){
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var PathUtils_1 = require("./PathUtils");
exports.PathUtils = PathUtils_1.default;

},{"./PathUtils":1}]},{},[2])(2)
});

//# sourceMappingURL=jibo-plugins.js.map
