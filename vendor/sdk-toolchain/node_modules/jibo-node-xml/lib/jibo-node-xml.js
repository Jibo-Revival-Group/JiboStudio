(function(f){if(typeof exports==="object"&&typeof module!=="undefined"){module.exports=f()}else if(typeof define==="function"&&define.amd){define([],f)}else{var g;if(typeof window!=="undefined"){g=window}else if(typeof global!=="undefined"){g=global}else if(typeof self!=="undefined"){g=self}else{g=this}g.jiboNodeXml = f()}})(function(){var define,module,exports;return (function e(t,n,r){function s(o,u){if(!n[o]){if(!t[o]){var a=typeof require=="function"&&require;if(!u&&a)return a(o,!0);if(i)return i(o,!0);var f=new Error("Cannot find module '"+o+"'");throw f.code="MODULE_NOT_FOUND",f}var l=n[o]={exports:{}};t[o][0].call(l.exports,function(e){var n=t[o][1][e];return s(n?n:e)},l,l.exports,e,t,n,r)}return n[o].exports}var i=typeof require=="function"&&require;for(var o=0;o<r.length;o++)s(r[o]);return s})({1:[function(require,module,exports){
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * @enum {String}
 */
var NodeType = (function () {
    function NodeType() {
    }
    return NodeType;
}());
NodeType.TEXT = 'TEXT';
NodeType.ROOT = 'ROOT';
exports.NodeType = NodeType;
var Node = (function () {
    /**
     * @param {NodeType} type The type the current node is.
     * @param {String} value The value stored in this current node.
     * @constructor
     */
    function Node(type, value) {
        this.type = type;
        this.value = value;
        this.children = [];
        /**
         * Ancestor nodes of the current node.  Can possess multiple
         * parents.
         * @type {Array.<Node>}
         */
        this.parents = [];
        /**
         * Attributes of the node, defined in XML as `<tag attribute="value">`,
         * where the key is the attribute name.
         * @type {Object.<String:String>}
         */
        this.att = new Map();
        /**
         * The name of the tag. e.g. `<foo/>` would return `foo`.
         * Only set on nodes that are parsed from XML text
         * @type {String}
         */
        this.tagName = null;
    }
    /**
     * @param {NodeType} type The type of node being created.
     * @param {String} value The value stored in this node.
     */
    Node.prototype.cloneShallow = function () {
        var n = new Node(this.type);
        this.att.forEach(function (val, key) {
            n.setAttribute(key, val);
        });
        return n;
    };
    /**
     * @return {Node} A copy of the current node its entire sub-tree.
     */
    Node.prototype.cloneDeep = function () {
        var n = new Node(this.type);
        this.att.forEach(function (val, key) {
            n.setAttribute(key, val);
        });
        this.children.forEach(function (c) {
            var newC = c.cloneDeep();
            n.addChild(newC);
        });
        return n;
    };
    /**
     * Returns the value of an attribute given its name.
     * @param {String} name The name of the attribute.
     * @return {AttType} The value of the attribute.
     */
    Node.prototype.getAttribute = function (name) {
        return this.att.get(name);
    };
    /**
     * Returns a numeric attribute.
     * @param {String} name The name of the attribute.
     * @return {number} The value of the attribute.
     */
    Node.prototype.getNumberAtt = function (name) {
        var val = this.att.get(name);
        var type = typeof val;
        if (type === 'string') {
            return Number.parseFloat(val);
        }
        else {
            return val;
        }
    };
    /**
     * Returns the value of an attribute given its name.
     * @param {String} name The name of the attribute.
     * @param {String} value The name of the attribute.
     */
    Node.prototype.setAttribute = function (name, value) {
        this.att.set(name, value);
    };
    /**
     * Returns the value of an attribute given its name.
     * @param {String} name The name of the attribute.
     * @param {Function.<AttType>} modifier A modifier function.
     */
    Node.prototype.modifyAttribute = function (name, modifier) {
        var oldVal = this.getAttribute(name);
        var newVal = modifier(oldVal);
        this.setAttribute(name, newVal);
    };
    /**
     * Whether or not the current node is a terminal leaf node without any
     * children.
     * @return {Boolean} If the current node is a terminal leaf node.
     */
    Node.prototype.isLeaf = function () {
        return this.children.length === 0;
    };
    /**
     * Attaches a child node to the current one.
     * @param {Node} node Node to attach as a child.
     */
    Node.prototype.addChild = function (node) {
        this.children.push(node);
        node.parents.push(this);
    };
    /**
     * Removes all the parents to the current node.
     */
    Node.prototype.clearParents = function () {
        this.parents = [];
    };
    /**
     * Disconnects all the child nodes from this current node.
     */
    Node.prototype.removeAllChildren = function () {
        var _this = this;
        // Remove us as parent of all children
        this.children.forEach(function (c) {
            var found = false;
            for (var i = 0; i < c.parents.length; i++) {
                if (c.parents[i] === _this) {
                    c.parents.splice(i, 1);
                    found = true;
                    break;
                }
            }
            if (!found) {
                throw Error('Didn\'t find this in child\'s parent list');
            }
        });
        this.children = [];
    };
    /**
     * Removes a child node from the current node.
     * @param {Node} child Child node to remove.
     */
    Node.prototype.removeChild = function (child) {
        // Remove child
        var found = false;
        for (var i = 0; i < this.children.length; i++) {
            if (this.children[i] === child) {
                this.children.splice(i, 1);
                found = true;
                break;
            }
        }
        if (!found) {
            throw Error('Didn\'t find child children list');
        }
        // Remove parent
        found = false;
        for (var i = 0; i < child.parents.length; i++) {
            if (child.parents[i] === this) {
                child.parents.splice(i, 1);
                found = true;
                break;
            }
        }
        if (!found) {
            throw Error('Didn\'t find this in child\'s parent list');
        }
    };
    /**
     * Returns the first child that matches a given filter criteria.
     * @param  {Function} filter Function to determine if child meets criteria.
     * @return {?Node} The first child node to meet the criteria or none.
     */
    Node.prototype.findFirstChild = function (filter) {
        for (var i = 0; i < this.children.length; i++) {
            var node = this.children[i];
            if (filter(node)) {
                return node;
            }
        }
    };
    Node.prototype.findFirstChildType = function (type, filter) {
        if (filter) {
            return this.findFirstChild(function (n) { return n.type === type && filter(n); });
        }
        else {
            return this.findFirstChild(function (n) { return n.type === type; });
        }
    };
    Node.prototype.findFirstDescendant = function (filter) {
        for (var i = 0; i < this.children.length; i++) {
            var node = this.children[i];
            var res = node._findFirstDescendantRec(filter);
            if (res) {
                return res;
            }
        }
    };
    Node.prototype._findFirstDescendantRec = function (filter) {
        if (filter(this)) {
            return this;
        }
        for (var i = 0; i < this.children.length; i++) {
            var node = this.children[i];
            var res = node._findFirstDescendantRec(filter);
            if (res) {
                return res;
            }
        }
    };
    Node.prototype.findFirstDescendantType = function (type, filter) {
        if (filter) {
            return this.findFirstDescendant(function (n) { return n.type === type && filter(n); });
        }
        else {
            return this.findFirstDescendant(function (n) { return n.type === type; });
        }
    };
    Node.prototype.findFirstParent = function (filter) {
        for (var i = 0; i < this.parents.length; i++) {
            var node = this.parents[i];
            if (filter(node)) {
                return node;
            }
        }
    };
    Node.prototype.findFirstParentType = function (type, filter) {
        if (filter) {
            return this.findFirstParent(function (n) { return n.type === type && filter(n); });
        }
        else {
            return this.findFirstParent(function (n) { return n.type === type; });
        }
    };
    Node.prototype.findFirstAncestor = function (filter) {
        for (var i = 0; i < this.parents.length; i++) {
            var node = this.parents[i];
            var res = node._findFirstAncestorRec(filter);
            if (res) {
                return res;
            }
        }
    };
    Node.prototype._findFirstAncestorRec = function (filter) {
        if (filter(this)) {
            return this;
        }
        for (var i = 0; i < this.parents.length; i++) {
            var node = this.parents[i];
            var res = node._findFirstAncestorRec(filter);
            if (res) {
                return res;
            }
        }
    };
    Node.prototype.findFirstAncestorType = function (type, filter) {
        if (filter) {
            return this.findFirstAncestor(function (n) { return n.type === type && filter(n); });
        }
        else {
            return this.findFirstAncestor(function (n) { return n.type === type; });
        }
    };
    Node.prototype.forEachAncestor = function (handler, filter) {
        for (var i = 0; i < this.parents.length; i++) {
            this.parents[i]._forEachAncestorRecursive(handler, filter);
        }
    };
    Node.prototype._forEachAncestorRecursive = function (handler, filter) {
        if (!filter || filter(this)) {
            handler(this);
        }
        for (var i = 0; i < this.parents.length; i++) {
            this.parents[i]._forEachAncestorRecursive(handler, filter);
        }
    };
    Node.prototype.forEachAncestorType = function (type, handler, filter) {
        if (filter) {
            this.forEachAncestor(handler, function (n) { return (n.type === type) && filter(n); });
        }
        else {
            this.forEachAncestor(handler, function (n) { return n.type === type; });
        }
    };
    /**
     * Iterates through all Descendants of node
     * @param {Function} handler
     * @param {Function} [filter]
     */
    Node.prototype.forEachDescendant = function (handler, filter) {
        for (var i = 0; i < this.children.length; i++) {
            this.children[i]._forEachDescendantRecursive(handler, filter);
        }
    };
    Node.prototype._forEachDescendantRecursive = function (handler, filter) {
        if (!filter || filter(this)) {
            handler(this);
        }
        for (var i = 0; i < this.children.length; i++) {
            this.children[i]._forEachDescendantRecursive(handler, filter);
        }
    };
    Node.prototype.forEachDescendantType = function (type, handler, filter) {
        if (filter) {
            this.forEachDescendant(handler, function (n) { return (n.type === type) && filter(n); });
        }
        else {
            this.forEachDescendant(handler, function (n) { return n.type === type; });
        }
    };
    Node.prototype.gatherChildren = function (filter) {
        var out = [];
        this.forEachChild(function (node) {
            out.push(node);
        }, filter);
        return out;
    };
    Node.prototype.gatherChildrenType = function (type, filter) {
        if (filter) {
            return this.gatherChildren(function (n) { return n.type === type && filter(n); });
        }
        else {
            return this.gatherChildren(function (n) { return n.type === type; });
        }
    };
    /**
     * @param {Function} filter Filtering function to apply to each node to
     *   determine if the node should be included in the returned list by
     *   returning true.
     * @return {List.<Node>}
     */
    Node.prototype.gatherDescendants = function (filter) {
        var out = [];
        this.forEachDescendant(function (node) {
            out.push(node);
        }, filter);
        return out;
    };
    Node.prototype.gatherDescendantsType = function (type, filter) {
        if (filter) {
            return this.gatherDescendants(function (n) { return n.type === type && filter(n); });
        }
        else {
            return this.gatherDescendants(function (n) { return n.type === type; });
        }
    };
    Node.prototype.gatherParents = function (filter) {
        var out = [];
        this.forEachParent(function (node) {
            out.push(node);
        }, filter);
        return out;
    };
    Node.prototype.gatherParentsType = function (type, filter) {
        if (filter) {
            return this.gatherParents(function (n) { return n.type === type && filter(n); });
        }
        else {
            return this.gatherParents(function (n) { return n.type === type; });
        }
    };
    Node.prototype.gatherAncestors = function (filter) {
        var out = [];
        this.forEachAncestor(function (node) {
            out.push(node);
        }, filter);
        return out;
    };
    Node.prototype.gatherAncestorsType = function (type, filter) {
        if (filter) {
            return this.gatherAncestors(function (n) { return n.type === type && filter(n); });
        }
        else {
            return this.gatherAncestors(function (n) { return n.type === type; });
        }
    };
    Node.prototype.forEachChildType = function (type, handler, filter) {
        if (filter) {
            this.forEachChild(handler, function (n) { return n.type === type && filter(n); });
        }
        else {
            this.forEachChild(handler, function (n) { return n.type === type; });
        }
    };
    Node.prototype.forEachChild = function (handler, filter) {
        for (var i = 0; i < this.children.length; i++) {
            var o = this.children[i];
            if (!filter || filter(o)) {
                handler(o);
            }
        }
    };
    Node.prototype.forEachParentType = function (type, handler, filter) {
        if (filter) {
            this.forEachParent(handler, function (n) { return n.type === type && filter(n); });
        }
        else {
            this.forEachParent(handler, function (n) { return n.type === type; });
        }
    };
    Node.prototype.forEachParent = function (handler, filter) {
        for (var i = 0; i < this.parents.length; i++) {
            var o = this.parents[i];
            if (!filter || filter(o)) {
                handler(o);
            }
        }
    };
    Node.prototype.toStringClosingTag = function () {
        return "</" + this.type + ">";
    };
    Node.prototype.toStringTagOnly = function (indents) {
        if (indents === void 0) { indents = 0; }
        var str = '';
        for (var i = 0; i < indents; i++) {
            str += ' ';
        }
        str += "<" + this.type;
        this.att.forEach(function (value, key) {
            str += " " + key + "=\"" + value + "\"";
        });
        if (this.value) {
            str += " value=\"" + this.value + "\"";
        }
        if (!this.children.length) {
            str += "/>";
        }
        else {
            str += ">";
        }
        return str;
    };
    /**
     * Prints the current tree.
     * @param {Boolean} [hierarchy]
     * @param {Number} [indents]
     * @return {String}
     * @override
     */
    Node.prototype.toString = function (hierarchy, indents) {
        //let str = `<type='${this.type}', numChildren=${this.children.length}`;
        if (hierarchy === void 0) { hierarchy = false; }
        if (indents === void 0) { indents = 0; }
        var str = this.toStringTagOnly(indents);
        if (this.children.length) {
            this.children.forEach(function (c) {
                if (hierarchy) {
                    str += '\n';
                }
                str += c.toString(hierarchy, indents + 4);
            });
            if (hierarchy) {
                str += '\n';
                for (var i = 0; i < indents; i++) {
                    str += ' ';
                }
            }
            str += this.toStringClosingTag();
        }
        return str;
    };
    Node.prototype.toStringHierarchy = function () {
        var lines = [];
        this._toStringHierarchy(0, lines);
        return lines.join('\n');
    };
    Node.prototype._toStringHierarchy = function (indents, lines) {
        var str = '';
        for (var i = 0; i < indents; i++) {
            str += ' ';
        }
        str += this.toString();
        if (this.children.length) {
            str += ', children=';
            lines.push(str);
            this.children.forEach(function (c) { return c._toStringHierarchy(indents + 4, lines); });
        }
        else {
            lines.push(str);
        }
    };
    return Node;
}());
exports.default = Node;

},{}],2:[function(require,module,exports){
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var Node_1 = require("./Node");
var Node_2 = require("./Node");
var PhraseType;
(function (PhraseType) {
    PhraseType[PhraseType["END"] = 0] = "END";
    PhraseType[PhraseType["START"] = 1] = "START";
    PhraseType[PhraseType["TEXT"] = 2] = "TEXT";
})(PhraseType = exports.PhraseType || (exports.PhraseType = {}));
;
var Phrase = (function () {
    function Phrase(type, value) {
        this.type = type;
        this.value = value;
    }
    return Phrase;
}());
exports.Phrase = Phrase;
var Parser = (function () {
    function Parser() {
    }
    /**
     * Parses an XML string to produce a tree structure.
     * @param {String} input XML string input to transform into a tree.
     * @return {Node} The tree containing the parsed XML.
     */
    Parser.parseXML = function (input) {
        var terms = input.split('<');
        var firstText = terms[0].trim();
        var phrases = [];
        if (firstText.length) {
            phrases.push(new Phrase(PhraseType.TEXT, firstText));
        }
        for (var i = 1; i < terms.length; i++) {
            var terms2 = terms[i].split('>');
            if (terms2.length !== 2) {
                throw Error('Parsing error');
            }
            var tag = terms2[0];
            var text = terms2[1];
            if (tag[0] === '/') {
                phrases.push(new Phrase(PhraseType.END, tag.substr(1)));
            }
            else {
                phrases.push(new Phrase(PhraseType.START, tag));
            }
            if (text.length) {
                phrases.push(new Phrase(PhraseType.TEXT, text));
            }
        }
        var root = new Node_1.default(Node_2.NodeType.ROOT);
        Parser._parseXMLInternal(phrases, 0, undefined, root);
        return root;
    };
    Parser._parseXMLInternal = function (phrases, index, inputTagName, parent) {
        for (var i = index; i < phrases.length; i++) {
            var phrase = phrases[i];
            if (phrase.type === PhraseType.START) {
                // If this is a closing tag also
                var isClosing = phrase.value[phrase.value.length - 1] === '/';
                if (isClosing) {
                    phrase.value = phrase.value.substr(0, phrase.value.length - 1).trim();
                }
                // Break up the tag
                var tagNameMatches = phrase.value.match(/([\w-]+)(?=\b)/g);
                var tagName = (tagNameMatches && tagNameMatches.length > 0) ? tagNameMatches[0] : '';
                var terms = phrase.value.match(/(\S+)=["']?((?:.(?!["']?\s+(?:\S+)=|[>"']))+.)["']?/g) || [];
                var newNode = new Node_1.default(tagName);
                newNode.tagName = tagName.trim();
                // Parse att
                for (var k = 0; k < terms.length; k++) {
                    var keyVal = terms[k].trim().split('=');
                    if (keyVal.length !== 2) {
                        throw Error('Parsing error, incorrect key value pair');
                    }
                    var attVal = keyVal[1];
                    if ((attVal[0] === '"' || attVal[0] === '\'') &&
                        (attVal[attVal.length - 1] === '"' || attVal[attVal.length - 1] === '\'')) {
                        attVal = attVal.substr(1, attVal.length - 2);
                    }
                    newNode.att.set(keyVal[0], attVal);
                }
                parent.addChild(newNode);
                if (isClosing) {
                    //i;
                }
                else {
                    i = Parser._parseXMLInternal(phrases, i + 1, newNode.type, newNode);
                }
            }
            else if (phrase.type === PhraseType.END) {
                if (phrase.value === inputTagName) {
                    return i;
                }
                else {
                    throw Error("Parsing error: Found orphaned closing tag '</" + phrase.value + ">'");
                }
            }
            else if (phrase.type === PhraseType.TEXT) {
                // We don't accept empty string text nodes (with white space)
                var trimmed = phrase.value.trim();
                if (trimmed.length > 0) {
                    parent.addChild(new Node_1.default(Node_2.NodeType.TEXT, phrase.value));
                }
            }
            else {
                throw Error("Shouldn't happen");
            }
        }
    };
    return Parser;
}());
exports.default = Parser;

},{"./Node":1}],3:[function(require,module,exports){
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var Node_1 = require("./Node");
var Node_2 = require("./Node");
exports.NodeType = Node_2.NodeType;
var Parser_1 = require("./Parser");
exports.Parser = Parser_1.default;
exports.default = Node_1.default;

},{"./Node":1,"./Parser":2}]},{},[3])(3)
});

//# sourceMappingURL=jibo-node-xml.js.map
