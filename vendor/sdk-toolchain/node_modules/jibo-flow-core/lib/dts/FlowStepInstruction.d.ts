/**
 * One instruction step of a FlowHarness.
 * This controls where in a running flow a harness step will occur, and what will happen when the step is executed.
 */
export declare class FlowStepInstruction {
    activity: string;
    class: string;
    flow: string;
    id: string;
    at: string;
    delay: number;
    stub: (activity, notepad, blackboard) => string;
    assert: (activity, notepad, blackboard) => boolean;
    do: (activity, notepad, blackboard) => string;
    say: string;
    it: string;
    ssm: any;
    executed: boolean;
    result: any;
    assertion: boolean;
    timestamp: number;
}
