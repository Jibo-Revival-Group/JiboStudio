import { FlowStep } from './FlowStep';
import { FlowStepInstruction } from './FlowStepInstruction';
/**
 * Class to produce Flow Harness entries via method chaining interface.
 *
 */
export declare class FlowHarness {
    ssm: any;
    private _steps;
    /**
     * Construct with the ssm, which supplies references to ASRService and TTSService.
     * The ssm will be in present in every step generated.
     * @param baseline
     */
    constructor(ssm: any);
    /**
     * Create a chainable-method flowstep object initialized to a copy of the baseline.
     * @returns {FlowStep}
     */
    when(): FlowStep;
    /**
     * Check all the harness' steps for consistency and return their instructions as an array.
     * @returns {any}
     */
    getSteps(): FlowStepInstruction[];
}
