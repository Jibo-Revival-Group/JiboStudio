import * as action from './action';
import * as motivation from './motivation';
import * as common from './common';
import * as goal from './goal';
import * as api from './api';
/**
 * @description Jibo Action System is a package that provides a Goal / Action abstraction
 * for Jibo's behavior. It contains "actions" as units of behavior that can be selected
 * to achieve goals through an action selection policy. Goals can be provided both internally
 * by goal providers and externally from skills or other systems
 *
 * @namespace jibo.action
 */
export { action, motivation, common, goal, api };
