export interface EventResult<T> {
    promise: Promise<T>;
    stop(): void;
}
export interface EmitterLike {
    emit(event: string, ...args: any[]): any;
    on(event: string, listener: Function): EmitterLike;
    once(event: string, listener: Function): EmitterLike;
    removeListener(event: string, listener: Function): EmitterLike;
}
export interface EventLike<T> {
    on(handler: (data?: T) => void): this;
    removeListener(handler: (data?: T) => void): this;
}
export declare type Handler<T> = (data?: T) => void;
export declare type Predicate<T> = (data?: T) => boolean;
export declare class EventWaiter<T> {
    private predicate;
    private eventName;
    private eventEmitter;
    private timeout;
    constructor(predicate: Predicate<T>, eventName: string, eventEmitter: EmitterLike, timeout?: number);
    start(): EventResult<T>;
}
export declare class TypedEventWaiter<T> {
    private predicate;
    private event;
    private timeout;
    constructor(predicate: Predicate<T>, event: EventLike<T>, timeout?: number);
    start(): EventResult<T>;
}
