import { HJData } from './Types';
import { ActionRuntime } from './ActionRuntime';
export declare namespace data {
}
export interface MemoryRecord<T> {
    time: Date;
    data: T;
}
export declare type MemoryKey = 'HJData';
export declare class Memory {
    private parent;
    private map;
    constructor(parent: ActionRuntime);
    /**
     * Returns the memory entry for HJData
     * @returns {MemoryRecord<HJData>}
     */
    getHJData(): MemoryRecord<HJData>;
    /**
     * Sets the memory entry for HJData
     * @param {HJData} data
     */
    setHJData(data: HJData): void;
    private set(key, data);
    private get(key);
}
