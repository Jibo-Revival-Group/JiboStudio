(function(f){if(typeof exports==="object"&&typeof module!=="undefined"){module.exports=f()}else if(typeof define==="function"&&define.amd){define([],f)}else{var g;if(typeof window!=="undefined"){g=window}else if(typeof global!=="undefined"){g=global}else if(typeof self!=="undefined"){g=self}else{g=this}g.jiboSync = f()}})(function(){var define,module,exports;return (function e(t,n,r){function s(o,u){if(!n[o]){if(!t[o]){var a=typeof require=="function"&&require;if(!u&&a)return a(o,!0);if(i)return i(o,!0);var f=new Error("Cannot find module '"+o+"'");throw f.code="MODULE_NOT_FOUND",f}var l=n[o]={exports:{}};t[o][0].call(l.exports,function(e){var n=t[o][1][e];return s(n?n:e)},l,l.exports,e,t,n,r)}return n[o].exports}var i=typeof require=="function"&&require;for(var o=0;o<r.length;o++)s(r[o]);return s})({1:[function(require,module,exports){
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Client_1 = require("./Client");
const Server_1 = require("./Server");
const Stopper_1 = require("./Stopper");
const Command_1 = require("./Command");
const stopper = new Stopper_1.default();
class API {
    static get Command() {
        return Command_1.default;
    }
    static createServer(port, dest, verbose, cb) {
        Server_1.default.start(port, dest, verbose, (error, msg) => {
            if (error) {
                console.log(error);
                cb(error);
            }
            else {
                console.log(msg);
                cb(null);
            }
        });
    }
    static uploadToServer(options, cb) {
        if (typeof options !== "object") {
            options = {
                url: arguments[0],
                dir: arguments[1],
                close: arguments[2],
                force: false,
                verbose: arguments[3],
            };
            cb = arguments[4];
        }
        stopper.reset();
        Client_1.default.push(options.url, options.dir, options.force, options.verbose, (error, msg) => {
            if (options.close) {
                Client_1.default.done(options.url, (_e) => {
                    if (_e && options.verbose) {
                        console.log(_e);
                    }
                    cb(error, msg);
                });
            }
            else {
                cb(error, msg);
            }
        });
    }
    static closeServer(url, cb) {
        Client_1.default.done(url, (error, msg) => {
            cb(error, msg);
        });
    }
    static stop() {
        stopper.stop();
    }
}
exports.default = API;

},{"./Client":2,"./Command":3,"./Server":6,"./Stopper":7}],2:[function(require,module,exports){
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const http = require("http");
const zlib = require("zlib");
const tarFs = require("tar-fs");
const Meter = require("stream-meter");
const request = require("request");
const chalk = require("chalk");
const Common_1 = require("./Common");
const FailErrors_1 = require("./FailErrors");
const Stopper_1 = require("./Stopper");
const jibo_syncignore_1 = require("jibo-syncignore");
let stopper = new Stopper_1.default();
function _resolvePath(_path) {
    if (typeof _path !== "string") {
        return ".";
    }
    return _path;
}
function _getServerList(host, port) {
    return new Promise((resolve, reject) => {
        if (stopper.doStop) {
            reject(FailErrors_1.default.STOPPED);
            return;
        }
        Common_1.default.startTimer('Generating server list');
        const postOptions = {
            host: host,
            port: port,
            path: '/checksum',
            method: 'POST'
        };
        const req = http.request(postOptions, (res) => {
            if (stopper.doStop) {
                reject(FailErrors_1.default.STOPPED);
                return;
            }
            res.setEncoding('utf8');
            let strData = '';
            res.on('data', (chunk) => {
                strData += chunk;
            });
            res.on('end', (err) => {
                if (stopper.doStop) {
                    reject(FailErrors_1.default.STOPPED);
                }
                else if (err) {
                    reject('Cannot connect to ' + host + ':' + port + ' or it is already in use.');
                }
                else {
                    const serverList = JSON.parse(strData);
                    Common_1.default.endTimer();
                    resolve(serverList);
                }
            });
        });
        req.end();
    });
}
function _generateAutoIgnoreFile(directory, force) {
    return new Promise((resolve, reject) => {
        if (stopper.doStop) {
            reject(FailErrors_1.default.STOPPED);
            return;
        }
        if (force) {
            Common_1.default.log(chalk.white('Force removing .syncignore-auto file.'));
        }
        Common_1.default.startTimer('Generating ignore list');
        const syncignore = new jibo_syncignore_1.SyncIgnore(directory);
        const packages = syncignore.exec(force);
        Common_1.default.endTimer();
        resolve(packages);
    });
}
function _getUpdateList(directory, serverList) {
    return new Promise((resolve, reject) => {
        if (stopper.doStop) {
            reject(FailErrors_1.default.STOPPED);
            return;
        }
        Common_1.default.generateChecksum(directory, true).then((clientList) => {
            if (stopper.doStop) {
                reject(FailErrors_1.default.STOPPED);
                return;
            }
            let deleteList = [];
            let updateList = [];
            for (let file in clientList) {
                if (serverList.hasOwnProperty(file)) {
                    if (serverList[file] !== clientList[file]) {
                        updateList.push(file);
                    }
                    delete serverList[file];
                }
                else {
                    updateList.push(file);
                }
            }
            for (let deleteFile in serverList) {
                deleteList.push(deleteFile);
            }
            resolve({ updateList, deleteList });
        }).catch((error) => {
            reject('Error retrieving update list:  ' + error);
        });
    });
}
function _upload(host, port, directory, updateList, deleteList) {
    return new Promise((resolve, reject) => {
        if (stopper.doStop) {
            reject(FailErrors_1.default.STOPPED);
            return;
        }
        if (deleteList.length === 0 && updateList.length === 0) {
            Common_1.default.log(chalk.gray("Everything is already up to date."));
            resolve({ size: 0, compSize: 0 });
            return;
        }
        _tryDelete(host, port, deleteList, (err) => {
            if (stopper.doStop) {
                reject(FailErrors_1.default.STOPPED);
                return;
            }
            if (err) {
                Common_1.default.log(chalk.red('Delete failed: ' + err));
            }
            _tryUpload(host, port, updateList, directory, (err, size, compSize) => {
                if (stopper.doStop) {
                    reject(FailErrors_1.default.STOPPED);
                }
                else if (err) {
                    reject(err);
                }
                else {
                    resolve({ size, compSize });
                }
            });
        });
    });
}
function _tryDelete(_host, _port, _deleteList, cb) {
    if (_deleteList.length > 0) {
        Common_1.default.startTimer('Deleting ' + _deleteList.length + ' files');
        const body = JSON.stringify({
            deleteList: _deleteList
        });
        const postOptions = {
            host: _host,
            port: _port,
            path: '/delete',
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'Content-Length': body.length
            }
        };
        const req = http.request(postOptions, (res) => {
            let str = '';
            res.on('data', (chunk) => {
                str += chunk;
            });
            res.on('end', () => {
                Common_1.default.endTimer();
                if (res.status === 'ERROR') {
                    cb(str);
                }
                else {
                    cb(null);
                }
            });
        });
        req.on('error', (e) => {
            cb(e);
        });
        req.write(body);
        req.end();
    }
    else {
        cb(null);
    }
}
function _tryUpload(_host, _port, updateList, directory, cb) {
    if (updateList.length > 0) {
        Common_1.default.startTimer('Updating ' + updateList.length + ' files');
        const upUrl = 'http://' + _host + ':' + _port + '/upload';
        const req = request.post(upUrl);
        const sizeMeter = new Meter();
        const compressedMeter = new Meter();
        const targzStream = tarFs.pack(directory, {
            entries: updateList,
            dereference: true
        });
        const stream = targzStream
            .pipe(sizeMeter)
            .pipe(zlib.createGzip({ level: 9 }))
            .pipe(compressedMeter)
            .pipe(req);
        stopper.once('stop', () => {
            stream.abort();
            cb(FailErrors_1.default.STOPPED);
        });
        stream.on('end', (err) => {
            Common_1.default.endTimer();
            cb(err, sizeMeter.bytes, compressedMeter.bytes);
        });
        stream.on('error', (err) => {
            cb(err, null, null);
        });
    }
    else {
        cb(null, 0, 0);
    }
}
class Client {
    static push(_url, _directory, force, verbose, cb) {
        Common_1.default.setVerbose(verbose);
        let urlData = Common_1.default.getHostPort(_url);
        if (!urlData) {
            cb('Error parsing url into host:port');
            return;
        }
        let host = urlData.host;
        let port = urlData.port;
        let directory = _resolvePath(_directory);
        let start = Date.now();
        Common_1.default.log(chalk.white.bold.underline('Working directory:') + ' ' + chalk.white(directory) + '\n');
        let _serverList = null;
        _getServerList(host, port).then((serverList) => {
            _serverList = serverList;
            return _generateAutoIgnoreFile(directory, force, verbose);
        }).then(() => {
            return _getUpdateList(directory, _serverList);
        }).then((lists) => {
            return _upload(host, port, directory, lists.updateList, lists.deleteList);
        }).then(() => {
            cb(null, 'Completed in ' + (Date.now() - start) / 1000 + ' seconds');
        }).catch((e) => {
            cb(e);
        });
    }
    static done(_url, cb) {
        let urlData = Common_1.default.getHostPort(_url);
        if (!urlData) {
            cb('Error parsing url into host:port', null);
            return;
        }
        let postOptions = {
            host: urlData.host,
            port: urlData.port,
            path: '/close',
            method: 'POST'
        };
        let req = http.request(postOptions, (res) => {
            let chunk = "";
            res.on('data', function (data) {
                chunk += (data);
            });
            res.on('end', () => {
                let data;
                try {
                    data = JSON.parse(chunk);
                }
                catch (e) {
                    res.writeHead(200, { 'Content-Type': 'text/html' });
                    res.write(JSON.stringify({
                        Status: 'ERROR',
                        Message: 'Invalid request to close sync server. Request body is not valid JSON'
                    }));
                    res.end();
                    return;
                }
                if (data.Status === 'ERROR') {
                    cb(data.Message, null);
                }
                else {
                    cb(null, data.Message);
                }
            });
        });
        req.end();
    }
}
Client.FAIL_ERRORS = FailErrors_1.default;
exports.default = Client;

},{"./Common":4,"./FailErrors":5,"./Stopper":7,"chalk":undefined,"http":undefined,"jibo-syncignore":undefined,"request":undefined,"stream-meter":undefined,"tar-fs":undefined,"zlib":undefined}],3:[function(require,module,exports){
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const API_1 = require("./API");
const path = require("path");
class Command {
    constructor(binDir) {
        this.allowedCommands = [
            ["create-server", "[port] [dest] [--verbose]", "Create a sync server on current IP with specified port and designated destination directory"],
            ["start", "[url] [source] [--verbose] [--regenerateIgnoreList]", "Sync source directory to specified server url (ip:port)"]
        ];
        this.allowedOptions = [
            ["-h", "--help", "Output usage information"],
            ["-V", "--version", "Output the version number"]
        ];
        this.rootPath = path.dirname(binDir);
        this.parse();
    }
    parse() {
        let found = false;
        let firstArg = process.argv[2];
        if (firstArg === this.allowedOptions[0][0] || firstArg === this.allowedOptions[0][1]) {
            found = true;
            this.printUsage();
        }
        let packageInfo = require(path.join(this.rootPath, 'package.json'));
        if (firstArg === this.allowedOptions[1][0] || firstArg === this.allowedOptions[1][1]) {
            found = true;
            console.log(`\n${packageInfo.version}\n`);
        }
        if (!found) {
            let cmdLength = this.allowedCommands.length;
            for (let cmdIdx = 0; cmdIdx < cmdLength; cmdIdx++) {
                let cmdToCheck = this.allowedCommands[cmdIdx][0];
                if (firstArg === cmdToCheck) {
                    found = true;
                    let args = process.argv.slice(3, process.argv.length);
                    this.processCommand(cmdToCheck, args, (error, msg) => {
                        if (error) {
                            console.log(`\n${error}`);
                            this.printUsage();
                        }
                        else {
                            console.log(`\n${msg}\n`);
                        }
                    });
                }
            }
        }
        if (!found) {
            this.printUsage();
        }
        return found;
    }
    printUsage() {
        let rightCol = 40;
        console.log("\n  Usage: sync [options] [command]\n\n");
        console.log("  Commands:\n");
        this.allowedCommands.forEach((command) => {
            let spaceCount = rightCol - command[0].length - command[1].length;
            if (spaceCount <= 0) {
                spaceCount = 4;
            }
            let spaces = '';
            for (var i = 0; i < spaceCount; i++) {
                spaces += ' ';
            }
            console.log(`    ${command[0]} ${command[1]}${spaces}${command[2]}`);
        });
        console.log("\n  Options:\n");
        this.allowedOptions.forEach((option) => {
            let sep = ', ';
            let spaceCountOpt = rightCol - option[0].length - option[1].length - sep.length;
            if (spaceCountOpt <= 0) {
                spaceCountOpt = 4;
            }
            let spacesOpt = '';
            for (let i = 0; i < spaceCountOpt; i++) {
                spacesOpt += ' ';
            }
            console.log(`    ${option[0]}${sep}${option[1]} ${spacesOpt}${option[2]}`);
        });
        console.log('');
    }
    processCommand(command, args, callback) {
        if (command === 'create-server') {
            let verbose = true;
            if (args[2] === '--verbose') {
                verbose = args[3];
            }
            API_1.default.createServer(args[0], args[1], verbose, (error, msg) => {
                callback(error, msg);
            });
        }
        else if (command === 'start') {
            let _verbose = true;
            let _regenerateIgnoreList = false;
            for (let a = 0; a < args.length; a++) {
                if (args[a] === '--verbose') {
                    _verbose = args[a + 1];
                }
                if (args[a] === '--regenerateIgnoreList') {
                    _regenerateIgnoreList = args[a + 1];
                }
            }
            var options = {
                url: args[0],
                directory: args[1],
                closeWhenDone: false,
                regenerateIgnoreList: _regenerateIgnoreList,
                verbose: _verbose
            };
            API_1.default.uploadToServer(options, (error, msg) => {
                callback(error, msg);
            });
        }
        else {
            callback(`Undefined command: ${command}`, null);
        }
    }
}
exports.default = Command;

},{"./API":1,"path":undefined}],4:[function(require,module,exports){
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const async = require("async");
const crypto = require("crypto");
const fs = require("fs");
const path = require("path");
const util = require("util");
const chalk = require("chalk");
const Stopper_1 = require("./Stopper");
const FailErrors_1 = require("./FailErrors");
const jibo_syncignore_1 = require("jibo-syncignore");
let useTimestamp = true;
let stopper = new Stopper_1.default();
function log() {
    Common.log.apply(Common, arguments);
}
function _findFilesRecursive(baseDir, currentDir, ignoreGlobs, ignorePaths, callback) {
    if (stopper.doStop) {
        callback(FailErrors_1.default.STOPPED);
        return;
    }
    fs.readdir(currentDir, (error, curFiles) => {
        if (error) {
            callback(error, null);
            return;
        }
        let files = [];
        let nextDirs;
        curFiles = curFiles.filter((name) => {
            const rootRelativePath = '/' + path.relative(baseDir, path.join(currentDir, name)).replace(/\\/g, '/');
            let dir = rootRelativePath;
            while (dir !== '/') {
                if (ignorePaths.indexOf(dir) > -1) {
                    return false;
                }
                dir = path.dirname(dir);
            }
            for (let i = 0, len = ignoreGlobs.length; i < len; ++i) {
                if (ignoreGlobs[i].test(rootRelativePath)) {
                    return false;
                }
            }
            return true;
        });
        nextDirs = curFiles.filter((name) => {
            if (!currentDir) {
                return false;
            }
            let filepath = path.join(currentDir, name);
            try {
                let stat = fs.statSync(filepath);
                return (stat && stat.isDirectory());
            }
            catch (e) {
                log(chalk.red("Error getting file info on " + filepath + "; skipping.."));
                return false;
            }
        });
        curFiles = curFiles.filter((name) => {
            if (!currentDir) {
                return false;
            }
            let filepath = path.join(currentDir, name);
            try {
                let stat = fs.statSync(filepath);
                return (stat && stat.isFile());
            }
            catch (e) {
                log(chalk.red("Error getting file info on " + filepath + "; skipping.."));
                return false;
            }
        });
        curFiles = curFiles.map((name) => {
            return path.join(currentDir, name);
        });
        files = files.concat(curFiles);
        let tasks = [];
        nextDirs.forEach((dir) => {
            tasks.push((cb) => {
                let nextDir = path.join(currentDir, dir);
                _findFilesRecursive(baseDir, nextDir, ignoreGlobs, ignorePaths, (error, newFiles) => {
                    files = files.concat(newFiles);
                    cb();
                });
            });
        });
        async.parallelLimit(tasks, 5, (error) => {
            if (stopper.doStop) {
                callback(FailErrors_1.default.STOPPED);
            }
            else if (error) {
                console.error('Error executing recursive file find: ' + error);
                callback(error, null);
            }
            else {
                callback(null, files);
            }
        });
    });
}
function _getChecksum(filename, cb) {
    if (!filename) {
        cb(null);
        return;
    }
    if (useTimestamp) {
        fs.stat(path.normalize(filename), (error, stats) => {
            if (stats) {
                let time = stats.mtime;
                if (util.isDate(time)) {
                    time = Math.trunc(time.getTime() / 1000);
                }
                cb(time);
                return;
            }
            log(chalk.red('Error getting checksum for ' + filename + ': ' + error));
            cb(null);
        });
    }
    else {
        fs.readFile(path.normalize(filename), (error, data) => {
            if (data) {
                let cksum = crypto.createHash('md5');
                cksum.update(data);
                cb(cksum.digest('hex'));
                return;
            }
            log(chalk.red('Error getting checksum for ' + filename + ': ' + error));
            cb(null);
        });
    }
}
Math.trunc = Math.trunc || function (x) {
    return x < 0 ? Math.ceil(x) : Math.floor(x);
};
function ignore(directory, file) {
    const ignorePath = path.resolve(directory, file);
    try {
        return fs.readFileSync(ignorePath, 'utf8')
            .split(/[\n\r]/g)
            .filter((item) => {
            return !!item;
        });
    }
    catch (e) {
        return [];
    }
}
class Common {
    static generateChecksum(directory, useIgnore) {
        return new Promise((resolve, reject) => {
            if (!directory) {
                reject(FailErrors_1.default.NO_DIRECTORY);
                return;
            }
            let autoIgnorePatterns = (useIgnore ? ignore(directory, jibo_syncignore_1.SyncIgnore.FILENAME) : []);
            let ignorePatterns = (useIgnore ? ignore(directory, jibo_syncignore_1.SyncIgnore.FILENAME_LOCAL) : []);
            ignorePatterns = autoIgnorePatterns.concat(ignorePatterns);
            let baseDir = directory.replace(/\/$/, '');
            fs.stat(baseDir, (error, stats) => {
                if (error || (stats && !stats.isDirectory())) {
                    let errorMsg = 'Directory ' + baseDir + ' does not exist.';
                    reject(errorMsg);
                    return;
                }
                Common.startTimer('Retrieving file list');
                const ignoreGlobs = ignorePatterns.filter((item) => {
                    if (item.indexOf('!') === 0) {
                        console.warn('WARNING: Includes are not longer supported in syncignore (%s)', item);
                        return false;
                    }
                    return item.indexOf('*') > -1;
                }).map((item) => {
                    return new RegExp('\\b' + item
                        .replace(/\//g, '\/')
                        .replace(/\./g, '\\.')
                        .replace(/([\/])?\*+([\/])?/g, '$1?.*$2')
                        .replace(/^\?/, '')
                        + '\\b');
                });
                const ignorePaths = ignorePatterns.filter((item) => {
                    return item.indexOf('*') === -1 && item[0] !== '!';
                }).map((item) => {
                    if (item[0] !== '/') {
                        item = '/' + item;
                    }
                    return item;
                });
                _findFilesRecursive(baseDir, baseDir, ignoreGlobs, ignorePaths, (error, _list) => {
                    if (error) {
                        reject(error);
                        return;
                    }
                    Common.endTimer();
                    let list = _list;
                    list = list.map((filepath) => {
                        return path.relative(baseDir, filepath);
                    });
                    let tasks = [];
                    let checksumList = {};
                    list.forEach((file) => {
                        tasks.push((_cb) => {
                            if (stopper.doStop) {
                                _cb(FailErrors_1.default.STOPPED);
                                return;
                            }
                            file = path.normalize(file);
                            let relativeFile = path.join(baseDir, file);
                            _getChecksum(relativeFile, (hash) => {
                                if (hash) {
                                    file = file.replace(/\\/g, "/");
                                    checksumList[file] = hash;
                                }
                                _cb();
                            });
                        });
                    });
                    Common.startTimer("Generating checksum list");
                    async.parallelLimit(tasks, 5, (error) => {
                        if (error) {
                            log(chalk.red('Error executing checksums: ' + error));
                            reject(error);
                        }
                        else {
                            Common.endTimer();
                            resolve(checksumList);
                        }
                    });
                });
            });
        });
    }
    static getHostPort(_url) {
        let strArray = _url.split(':');
        if (strArray.length !== 2) {
            log(chalk.red('Url is not specified as [IP:port]: ' + _url));
            return undefined;
        }
        let host = strArray[0];
        let port = Number(strArray[1]);
        if (isNaN(port)) {
            log(chalk.red('Invalid port defined ' + port));
            return undefined;
        }
        return {
            "host": host,
            "port": port
        };
    }
    static setVerbose(enable) {
        Common.verboseLog = enable;
    }
    static log() {
        if (Common.verboseLog) {
            console.log.apply(console, arguments);
        }
    }
    static startTimer(str) {
        if (Common.verboseLog) {
            process.stdout.write(chalk.white(str) + ' ... ');
            Common._start = Date.now();
        }
    }
    static endTimer() {
        if (Common.verboseLog) {
            console.log(chalk.white('done.'), chalk.gray((Date.now() - Common._start) / 1000), chalk.gray('secs'));
            Common._start = null;
        }
    }
    static error(str) {
        if (Common.verboseLog) {
            console.error(str);
        }
    }
}
Common._start = null;
Common.verboseLog = false;
exports.default = Common;

},{"./FailErrors":5,"./Stopper":7,"async":undefined,"chalk":undefined,"crypto":undefined,"fs":undefined,"jibo-syncignore":undefined,"path":undefined,"util":undefined}],5:[function(require,module,exports){
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = {
    NO_PORT_SPECIFIED: 'NO_PORT_SPECIFIED',
    CANNOT_CONNECT: 'CANNOT_CONNECT',
    NO_DIRECTORY: 'No directory was specified. Aborting checksum.',
    ERROR: 'ERROR',
    STOPPED: 'STOPPED'
};

},{}],6:[function(require,module,exports){
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const async = require("async");
const fs = require("fs-extra");
const http = require("http");
const Meter = require("stream-meter");
const path = require("path");
const tarFs = require("tar-fs");
const zlib = require("zlib");
const Common_1 = require("./Common");
let server = undefined;
let port = undefined;
let uploadDir = '.';
const URLUPLOAD = '/upload';
const URLDELETE = '/delete';
const URLCHECKSUM = '/checksum';
const URLCLOSE = '/close';
const URLPING = '/ping';
const exec = require('child_process').exec;
function _handleRequest(request, response) {
    if (request.method !== 'POST') {
        return;
    }
    switch (request.url) {
        case URLUPLOAD: {
            _upload(request, response);
            break;
        }
        case URLDELETE: {
            _delete(request, response);
            break;
        }
        case URLCHECKSUM: {
            _getChecksum(request, response);
            break;
        }
        case URLCLOSE: {
            _closeServer(request, response);
            break;
        }
        case URLPING: {
            _pingServer(request, response);
            break;
        }
        default: {
            response.writeHead(200, { 'Content-Type': 'text/html' });
            response.write(JSON.stringify({
                Status: 'ERROR',
                Message: 'Undefined url: ' + request.url
            }));
            response.end();
            break;
        }
    }
}
function stop(cb) {
    if (server) {
        server.close();
        server = undefined;
        cb(null, 'Server closed on port ' + port);
    }
    else {
        cb('No server to close', null);
    }
}
function _getChecksum(req, res) {
    if (req.method === 'POST') {
        Common_1.default.generateChecksum(uploadDir, false).then((ckList) => {
            res.writeHead(200, { 'Content-Type': 'text/html' });
            res.write(JSON.stringify(ckList));
            res.end();
        }).catch((error) => {
            res.writeHead(200, { 'Content-Type': 'text/html' });
            res.write(JSON.stringify({
                Status: 'ERROR',
                Message: error
            }));
            res.end();
        });
    }
}
function _setPermissions(callback) {
    exec('chown -R root:root /opt/jibo/Jibo/Skills && chmod -R 755 /opt/jibo/Jibo/Skills', (error) => {
        if (error) {
            Common_1.default.log('Error setting permissions', error);
            return callback(error);
        }
        console.log(`Permissions set`);
        callback();
    });
}
function _upload(req, res) {
    Common_1.default.log('Receiving upload...');
    let startTime = Date.now();
    if (req.method === 'POST') {
        _untar(req, uploadDir, (err, bytes, compressedBytes) => {
            _setPermissions((err) => {
                if (!err) {
                    Common_1.default.log('\ttook ' + (Date.now() - startTime) / 1000 + ' secs');
                    Common_1.default.log('\t' + bytes + ' bytes uploaded (' + compressedBytes + ' compressed)');
                }
                res.writeHead(200, "OK", { 'Content-Type': 'text/html' });
                res.end();
            });
        });
    }
}
function _untar(stream, directory, callback) {
    fs.ensureDirSync(directory);
    let sizeMeter = new Meter();
    let compressedMeter = new Meter();
    let extractOpts = { dmode: 0o755, fmode: 0o755 };
    stream.pipe(compressedMeter)
        .pipe(zlib.createGunzip())
        .pipe(sizeMeter)
        .pipe(tarFs.extract(directory, extractOpts))
        .on('finish', (err) => {
        if (err) {
            Common_1.default.log('Problem extracting tar stream', err);
            callback(err);
        }
        else {
            callback(null, sizeMeter.bytes, compressedMeter.bytes);
        }
    });
    stream.on('error', (err) => {
        if (err) {
            Common_1.default.log('problem with POST stream', err);
        }
        callback(err);
    });
}
function _delete(req, res) {
    Common_1.default.log('Receiving delete request...');
    let startTime = Date.now();
    if (req.method === 'POST') {
        let body = "";
        req.on('data', (chunk) => {
            body += chunk;
        });
        req.on('end', () => {
            let data = undefined;
            try {
                data = JSON.parse(body);
            }
            catch (error) {
                res.writeHead(200, { 'Content-Type': 'text/html' });
                res.write(JSON.stringify({
                    Status: 'ERROR',
                    Message: 'Invalid delete request. Request body is not valid JSON'
                }));
                res.end();
                return;
            }
            let list = data.deleteList;
            if (list !== undefined) {
                let tasks = [];
                list.forEach((file) => {
                    tasks.push((cb) => {
                        let filePath = path.resolve(uploadDir, file);
                        fs.remove(filePath, cb);
                    });
                });
                async.parallelLimit(tasks, 5, (error) => {
                    if (error) {
                        Common_1.default.log('Error executing deletes: ' + error);
                        res.writeHead(200, "OK", { 'Content-Type': 'text/html' });
                        res.write(JSON.stringify({
                            Status: 'ERROR',
                            Message: error
                        }));
                        res.end();
                    }
                    else {
                        res.writeHead(200, "OK", { 'Content-Type': 'text/html' });
                        res.end();
                    }
                    Common_1.default.log('\ttook ' + (Date.now() - startTime) / 1000 + ' secs');
                });
            }
        });
    }
}
function _closeServer(req, res) {
    stop((error, msg) => {
        if (error) {
            Common_1.default.log(error);
            res.writeHead(200, { 'Content-Type': 'text/html' });
            res.write(JSON.stringify({
                Status: 'ERROR',
                Message: error
            }));
            res.end();
        }
        else {
            Common_1.default.log(msg);
            res.writeHead(200, "OK", { 'Content-Type': 'text/html' });
            res.write(JSON.stringify({
                Status: 'OK',
                Message: msg
            }));
            res.end();
        }
    });
}
function _pingServer(req, res) {
    if (req.method === 'POST') {
        res.writeHead(200, "OK", { 'Content-Type': 'text/html' });
        res.end();
    }
}
class Server {
    static start(_port, _dest, verbose, cb) {
        Common_1.default.setVerbose(verbose);
        port = Number(_port);
        if (isNaN(port)) {
            cb('\nInvalid port defined: ' + port + ' Aborting.', null);
            return;
        }
        if (server) {
            if (_dest) {
                uploadDir = _dest;
            }
            cb(null, '\nSync server already listening on port ' + port);
        }
        else {
            if (_dest) {
                uploadDir = _dest;
            }
            server = http.createServer(_handleRequest);
            try {
                server.listen(port, "0.0.0.0", (error) => {
                    if (error) {
                        server.close();
                        server = undefined;
                        cb(error, null);
                    }
                    else {
                        cb(null, '\nSync server listening on port ' + port + '. Press CTRL+C or call stop() to exit.\n');
                    }
                });
                server.timeout = 0;
            }
            catch (error) {
                server.close();
                server = undefined;
                cb(error, null);
            }
        }
    }
}
exports.default = Server;

},{"./Common":4,"async":undefined,"child_process":undefined,"fs-extra":undefined,"http":undefined,"path":undefined,"stream-meter":undefined,"tar-fs":undefined,"zlib":undefined}],7:[function(require,module,exports){
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const events_1 = require("events");
class Stopper extends events_1.EventEmitter {
    constructor() {
        super();
        this._doStop = false;
    }
    stop() {
        this._doStop = true;
        this.emit('stop');
    }
    get doStop() {
        return this._doStop;
    }
    reset() {
        this.removeAllListeners();
        this._doStop = false;
    }
}
exports.default = Stopper;

},{"events":undefined}],8:[function(require,module,exports){
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const API_1 = require("./API");
exports.default = API_1.default;
module.exports = API_1.default;

},{"./API":1}]},{},[8])(8)
});

//# sourceMappingURL=jibo-sync.js.map
