(function(f){if(typeof exports==="object"&&typeof module!=="undefined"){module.exports=f()}else if(typeof define==="function"&&define.amd){define([],f)}else{var g;if(typeof window!=="undefined"){g=window}else if(typeof global!=="undefined"){g=global}else if(typeof self!=="undefined"){g=self}else{g=this}g.index = f()}})(function(){var define,module,exports;return (function e(t,n,r){function s(o,u){if(!n[o]){if(!t[o]){var a=typeof require=="function"&&require;if(!u&&a)return a(o,!0);if(i)return i(o,!0);var f=new Error("Cannot find module '"+o+"'");throw f.code="MODULE_NOT_FOUND",f}var l=n[o]={exports:{}};t[o][0].call(l.exports,function(e){var n=t[o][1][e];return s(n?n:e)},l,l.exports,e,t,n,r)}return n[o].exports}var i=typeof require=="function"&&require;for(var o=0;o<r.length;o++)s(r[o]);return s})({1:[function(require,module,exports){
var ErrorService = React.createClass({
    getInitialState: function () {
        var _this = this;
        this._selectedErrorInfos = [];
        this._origin = location.origin;
        this._errorMap = [];
        this._errorKeys = [];
        this._currentErrorServiceErrorId = "";
        this._priorityQueueErrors = [];
        this._reschedulerErrors = [];
        this.getErrorCodeData();
        // set the render method to update on an interval so we can update the dom based on the connectivity state of the socket
        setInterval(function () {
            ReactDOM.render(_this.render(), document.getElementById('error-service'));
        }, 1000);
        return {};
    },
    getCurrentErrorServiceError: function () {
        var _this = this;
        var getCurrentErrorIdUrl = this._origin + '/getCurrentErrorId';
        var request = new XMLHttpRequest();
        request.open("POST", getCurrentErrorIdUrl, true);
        request.onreadystatechange = function () {
            // request is complete
            if (request.readyState === 4) {
                // "OK" status
                if (request.status === 200) {
                    var response = JSON.parse(request.response);
                    _this._currentErrorServiceErrorId = response.currentErrorId;
                }
                else {
                    console.error("cannot get current error id data");
                }
            }
        };
        request.send();
    },
    getErrorCodeData: function () {
        var _this = this;
        var errorCodeDataUrl = this._origin + '/errorCodeData';
        var request = new XMLHttpRequest();
        request.open("POST", errorCodeDataUrl, true);
        request.onreadystatechange = function () {
            // request is complete
            if (request.readyState === 4) {
                // "OK" status
                if (request.status === 200) {
                    var response = JSON.parse(request.response);
                    _this._errorMap = response.errorList;
                    _this._errorKeys = Object.keys(_this._errorMap);
                    _this._errorKeys.sort();
                }
                else {
                    console.error("cannot get error code data");
                }
            }
        };
        request.send();
    },
    getErrorServiceContents: function () {
        var _this = this;
        var getContentsUrl = this._origin + '/getContents';
        var request = new XMLHttpRequest();
        request.open("POST", getContentsUrl, true);
        request.onreadystatechange = function () {
            // request is complete
            if (request.readyState === 4) {
                // "OK" status
                if (request.status === 200) {
                    var response = JSON.parse(request.response);
                    var priorityQueueJSONArray = JSON.parse(response.priorityQueueErrors);
                    _this._priorityQueueErrors = [];
                    for (var i = 0; i < priorityQueueJSONArray.length; ++i) {
                        var errorInfo = priorityQueueJSONArray[i];
                        var errorInfoPre = React.createElement("pre", { id: errorInfo._id, key: errorInfo._id }, JSON.stringify(errorInfo, null, 4));
                        _this._priorityQueueErrors.push(errorInfoPre);
                    }
                    var reschedulerJSONArray = JSON.parse(response.reschedulerErrors);
                    _this._reschedulerErrors = [];
                    for (var i = 0; i < reschedulerJSONArray.length; ++i) {
                        var errorInfo = reschedulerJSONArray[i];
                        var errorInfoPre = React.createElement("pre", { id: errorInfo._id, key: errorInfo._id }, JSON.stringify(errorInfo, null, 4));
                        _this._reschedulerErrors.push(errorInfoPre);
                    }
                }
                else {
                    console.error("cannot get priority queue data");
                }
            }
        };
        request.send();
    },
    sendMockCodes: function (enabled) {
        // TODO: eventually this will need to be an array of ints to mimic what we would actually be sending the platform side.
        // this is currently blocked by not having a mapping of error string to int on the SSM side
        var activeErrorArray = [];
        var node = document.getElementById("right");
        for (var i = 0; i < node.childNodes.length; ++i) {
            var optionNode = node.childNodes[i];
            activeErrorArray.push(optionNode.id);
        }
        // request the data for the system monitoring service
        var mockCodesUrl = this._origin + '/mockErrorCodes';
        var request = new XMLHttpRequest();
        request.open("POST", mockCodesUrl, true);
        request.send(JSON.stringify({
            enabled: enabled,
            codes: activeErrorArray
        }));
    },
    sortById: function (a, b) {
        var aId = (typeof a.hasAttribute === "function") && a.hasAttribute('id') ? a.id : a.props.id;
        var bId = (typeof b.hasAttribute === "function") && b.hasAttribute('id') ? b.id : b.props.id;
        return aId.localeCompare(bId);
    },
    moveRight: function () {
        var node = document.getElementById("left");
        for (var i = 0; i < node.childNodes.length; i++) {
            var optionNode = node.childNodes[i];
            if (optionNode.selected) {
                document.getElementById("right").appendChild(optionNode.cloneNode(true));
                node.removeChild(optionNode);
                --i;
            }
        }
        var sortedErrors = $("#right").children().sort(this.sortById);
        $("#right").html(sortedErrors);
        this.selectError();
        var errorsEnabled = sortedErrors.length > 0;
        this.sendMockCodes(errorsEnabled);
    },
    moveLeft: function () {
        var node = document.getElementById("right");
        for (var i = 0; i < node.childNodes.length; i++) {
            var optionNode = node.childNodes[i];
            if (optionNode.selected) {
                document.getElementById("left").appendChild(optionNode.cloneNode(true));
                node.removeChild(optionNode);
                --i;
            }
        }
        var sortedErrors = $("#left").children().sort(this.sortById);
        $("#left").html(sortedErrors);
        this.selectError();
        var errorsEnabled = node.childNodes.length > 0;
        this.sendMockCodes(errorsEnabled);
    },
    moveAllRight: function () {
        var node = document.getElementById("left");
        while (node.childNodes.length > 0) {
            document.getElementById("right").appendChild(node.firstChild.cloneNode(true));
            node.removeChild(node.firstChild);
        }
        var sortedErrors = $("#right").children().sort(this.sortById);
        $("#right").html(sortedErrors);
        this.selectError();
        var errorsEnabled = sortedErrors.length > 0;
        this.sendMockCodes(errorsEnabled);
    },
    moveAllLeft: function () {
        var node = document.getElementById("right");
        while (node.childNodes.length > 0) {
            document.getElementById("left").appendChild(node.firstChild.cloneNode(true));
            node.removeChild(node.firstChild);
        }
        var sortedErrors = $("#left").children().sort(this.sortById);
        $("#left").html(sortedErrors);
        this.selectError();
        var errorsEnabled = node.childNodes.length > 0;
        this.sendMockCodes(errorsEnabled);
    },
    updateErrorServiceInfo: function () {
        this.getCurrentErrorServiceError();
        this.getErrorServiceContents();
    },
    render: function () {
        var errorCodeOptions = [];
        this._errorKeys.sort();
        for (var i = 0; i < this._errorKeys.length; ++i) {
            var error = this._errorKeys[i];
            errorCodeOptions.push(React.createElement("option", { id: error, key: error }, error));
        }
        var renderHtml = React.createElement("div", null,
            React.createElement("h1", null, "Error Service Debug Page"),
            React.createElement("label", null,
                React.createElement("input", { type: "checkbox", onChange: this.disableSkillSwitch }),
                "Disable error service skill switch",
                React.createElement("br", null),
                React.createElement("input", { type: "button", onClick: this.updateErrorServiceInfo, value: "Refresh" })),
            React.createElement("table", null,
                React.createElement("tbody", null,
                    React.createElement("tr", null,
                        React.createElement("td", null,
                            React.createElement("text", null, "Error List"),
                            React.createElement("br", null),
                            React.createElement("select", { multiple: "multiple", id: "left", style: { height: 500, width: 300 }, onChange: this.selectError }, errorCodeOptions)),
                        React.createElement("td", null,
                            React.createElement("input", { type: "button", value: "<", onClick: this.moveLeft }),
                            React.createElement("input", { type: "button", value: "<<", onClick: this.moveAllLeft }),
                            React.createElement("input", { type: "button", value: ">>", onClick: this.moveAllRight }),
                            React.createElement("input", { type: "button", value: ">", onClick: this.moveRight })),
                        React.createElement("td", null,
                            React.createElement("text", null, "Active Errors"),
                            React.createElement("br", null),
                            React.createElement("select", { multiple: "multiple", id: "right", style: { height: 500, width: 300 }, onChange: this.selectError }))))),
            React.createElement("div", { id: "current-error" },
                React.createElement("text", null,
                    React.createElement("h3", null, "Current Error Service Error:"),
                    this._currentErrorServiceErrorId)),
            React.createElement("div", { id: "priority-queue-errors" },
                React.createElement("text", null,
                    React.createElement("h3", null, "Current Priority Queue Errors:"),
                    this._priorityQueueErrors)),
            React.createElement("div", { id: "rescheduler-errors" },
                React.createElement("text", null,
                    React.createElement("h3", null, "Rescheduler Errors:"),
                    this._reschedulerErrors)),
            React.createElement("div", { id: "error-info" },
                React.createElement("text", null,
                    React.createElement("h2", null, "Error Info")),
                this._selectedErrorInfos));
        return renderHtml;
    },
    selectError: function () {
        this._selectedErrorInfos = [];
        var errorInfoTextArea = $("#error-info");
        errorInfoTextArea.innerHTML = "";
        this.displayErrorInfoForSelectedElements(document.getElementById("left"));
        this.displayErrorInfoForSelectedElements(document.getElementById("right"));
        this._selectedErrorInfos.sort(this.sortById);
    },
    disableSkillSwitch: function (event) {
        var isChecked = event.target.checked;
        var disableSkillSwitchingUrl = this._origin + '/disableSkillSwitching';
        var request = new XMLHttpRequest();
        request.open("POST", disableSkillSwitchingUrl, true);
        request.send(JSON.stringify({
            disabled: isChecked,
        }));
    },
    displayErrorInfoForSelectedElements: function (node) {
        for (var i = 0; i < node.childNodes.length; i++) {
            var optionNode = node.childNodes[i];
            if (optionNode.selected) {
                var errorInfo = this._errorMap[optionNode.id];
                var errorInfoPre = React.createElement("pre", { id: errorInfo._id, key: errorInfo._id }, JSON.stringify(errorInfo, null, 4));
                this._selectedErrorInfos.push(errorInfoPre);
            }
        }
    }
});
$(document).ready(function () {
    // the error service needs to be started first so it can receive the message for which port the system-monitoring-service-sim is being hosted
    ReactDOM.render(React.createElement(ErrorService, null), document.getElementById('error-service'));
});

},{}]},{},[1])(1)
});

//# sourceMappingURL=index.js.map
