(function(f){if(typeof exports==="object"&&typeof module!=="undefined"){module.exports=f()}else if(typeof define==="function"&&define.amd){define([],f)}else{var g;if(typeof window!=="undefined"){g=window}else if(typeof global!=="undefined"){g=global}else if(typeof self!=="undefined"){g=self}else{g=this}g.jiboSyncignore = f()}})(function(){var define,module,exports;return (function e(t,n,r){function s(o,u){if(!n[o]){if(!t[o]){var a=typeof require=="function"&&require;if(!u&&a)return a(o,!0);if(i)return i(o,!0);var f=new Error("Cannot find module '"+o+"'");throw f.code="MODULE_NOT_FOUND",f}var l=n[o]={exports:{}};t[o][0].call(l.exports,function(e){var n=t[o][1][e];return s(n?n:e)},l,l.exports,e,t,n,r)}return n[o].exports}var i=typeof require=="function"&&require;for(var o=0;o<r.length;o++)s(r[o]);return s})({1:[function(require,module,exports){
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const path = require("path");
const fs = require("fs");
const SyncIgnore_1 = require("./SyncIgnore");
/**
 * The results when searching for packages.
 * @class SearchResults
 * @private
 */
class SearchResults {
    /**
     * @constructor
     */
    constructor(cwd) {
        this._cwd = cwd;
        this._dev = [];
        this._prod = [];
        this._all = [];
    }
    /**
     * Remove the redundant options and excludes
     * any production modules.
     * @method SearchResult#process
     * @return {string[]} List of packages ignore list
     */
    process() {
        // If it isn't in the production list
        // and it isn't in the dev list, consider a dev dependency
        this._all.forEach((curr) => {
            if (this._prod.indexOf(curr) < 0 && this._dev.indexOf(curr) < 0) {
                this._dev.push(curr);
            }
        });
        return this._dev.filter((item) => {
            // Do not add modules which are resolved above
            // the current working directory
            return item.slice(0, 4) !== path.sep + '..' + path.sep;
        }).filter((item) => {
            // Remove any production dependencies
            // usually this is because they were de-duped
            // and are shared between dev and prod deps
            return this._prod.indexOf(item) === -1;
        }).map((item) => {
            // normalize Windows paths
            return item.replace(/\\/g, '/');
        }).sort();
    }
    /**
     * For sub modules, we'll add the .syncignore file to ignore
     * any local files that shouldn't be synced.
     * @method SearchResult#appendFile
     * @param {string} dir Root directory of module
     */
    appendFile(dir) {
        const syncIgnorePath = path.join(dir, SyncIgnore_1.default.FILENAME_LOCAL);
        // Check that file exists
        if (!fs.existsSync(syncIgnorePath)) {
            return;
        }
        const list = fs.readFileSync(syncIgnorePath, 'utf8').split('\n');
        this.appendDevList(list, dir);
    }
    /**
     * Add a list of item to append to dev
     * @method SearchResult#appendDevList
     * @param {string[]} list The list of entries from .syncignore
     * @param {string} [dir] Root directory of module if list are relative
     */
    appendDevList(list, dir) {
        list.forEach(ignore => {
            if (ignore) {
                if (dir) {
                    const sep = ignore[0] !== path.sep ? path.sep : '';
                    this.appendDev(dir + sep + ignore);
                }
                else {
                    this.appendDev(ignore);
                }
            }
        });
    }
    /**
     * Add the list of remaining files.
     * @method SearchResult#appendAll
     * @param {string[]} list The list of relative entries from .syncignore
     */
    appendAll(list, dir) {
        list.forEach(ignore => {
            if (ignore) {
                const modulePath = path.join(dir, ignore);
                const curr = path.sep + path.relative(this._cwd, modulePath);
                this._all.push(curr);
            }
        });
    }
    /**
     * Add to the list of dev dependencies we should ignore
     * @method SearchResult#appendDev
     * @param {string} modulePath The root directory
     */
    appendDev(modulePath) {
        const curr = path.sep + path.relative(this._cwd, modulePath);
        // Check for duplicates
        if (this._dev.indexOf(curr) < 0) {
            this._dev.push(curr);
        }
    }
    /**
     * Add to the list of production dependencies we should not ignore
     * @method SearchResult#appendProd
     * @param {string} modulePath The root directory
     */
    appendProd(modulePath) {
        const curr = path.sep + path.relative(this._cwd, modulePath);
        if (this._prod.indexOf(curr) < 0) {
            this._prod.push(curr);
        }
    }
    /**
     * Destroy and don't use after this
     * @method SearchResult#destroy
     */
    destroy() {
        this._dev = null;
        this._prod = null;
        this._cwd = null;
    }
}
exports.default = SearchResults;

},{"./SyncIgnore":2,"fs":undefined,"path":undefined}],2:[function(require,module,exports){
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const path = require("path");
const fs = require("fs");
const minimist = require("minimist");
const chalk = require("chalk");
const resolve = require("resolve");
const SearchResults_1 = require("./SearchResults");
/**
 * This class is responsible for recursively searching through the
 * packages in the specified root folder (and all packages it links to),
 * and generating a list of ignore-able dependencies. Any package that is
 * a development dependency, but not a production dependency, will be outputted
 * as part of an ignore list.
 *
 * Since the process of generating an ignore list by searching dependencies is
 * a little slow, the results will be cached in an .syncignore-auto file, which will be
 * used on the next execution. However, callers can use a "--force" option to
 * override this and require the search.
 * @class SyncIgnore
 * @param  {string} cwd working folder to use; otherwise use current working directory
 */
class SyncIgnore {
    /**
     * Get the default file name
     * @name SyncIgnore.FILENAME
     * @default .syncignore-auto
     * @type {string}
     */
    static get FILENAME() {
        return '.syncignore-auto';
    }
    /**
     * Get the default file name for manual files.
     * @name SyncIgnore.FILENAME_LOCAL
     * @default .syncignore
     * @type {string}
     */
    static get FILENAME_LOCAL() {
        return '.syncignore';
    }
    /**
     * This is helper node.js script for launching the SyncIgnore utility. It can be used
     * for testing.
     *
     * Run it from the package that you want to generate an ignore list for. The output
     * will be a list of files sent to stdout. The same list will appear in the .syncignore-auto
     * file.
     *
     * Parameters:
     *   --force: forces the search process; if not set, the contents of .syncignore-auto
     *            (if present) will be used.
     *   --verbose or --debug: print out debugging info
     *   --empty: creates an empty .syncignore-auto file
     * @method SyncIgnore.cli
     */
    static cli() {
        const args = minimist(process.argv.slice(2), {
            boolean: [
                'force',
                'debug',
                'verbose',
                'empty'
            ],
            alias: {
                f: 'force',
                d: 'debug',
                e: 'empty',
                v: 'verbose'
            },
            default: {
                force: false,
                debug: false,
                empty: false,
                verbose: false
            }
        });
        const dir = args._[0] || process.cwd();
        const syncignore = new SyncIgnore(path.resolve(dir));
        const debug = args.debug || args.verbose;
        let packages;
        if (args.empty) {
            packages = syncignore.fakeExec();
        }
        else {
            packages = syncignore.exec(args.force, debug);
        }
        if (debug) {
            console.log(chalk.gray(packages.join('\n')), '\n');
            console.log(chalk.bold.yellow('Contains %d files to ignore.\n'), packages.length);
        }
        else {
            console.log(packages.join('\n'));
        }
    }
    /**
     * Constructs SyncIgnore instance
     */
    constructor(cwd) {
        this._cwd = cwd || process.cwd();
        this._debug = false;
    }
    /**
     * Execute SyncIgnore program
     * @method SyncIgnore#exec
     * @param  {boolean}  force force full search, even if syncignore-auto file
     *                          exists
     * @param  {boolean}  debug if true, debugging info will be printed to
     *                          console log
     * @return {string[]} array of package names (to ignore)
     */
    exec(force, debug) {
        this._debug = debug;
        const cachePath = path.join(this._cwd, SyncIgnore.FILENAME);
        if (force || !fs.existsSync(cachePath)) {
            if (debug) {
                console.log(chalk.green("\nSearching node_modules for files to ignore...\n"));
            }
            const results = new SearchResults_1.default(this._cwd);
            this._search(this._cwd, results);
            const packages = results.process();
            fs.writeFileSync(cachePath, packages.join('\n'));
            results.destroy();
            return packages;
        }
        else {
            if (debug) {
                console.log(chalk.green("Using %s cached ignore file"), SyncIgnore.FILENAME);
            }
            const data = fs.readFileSync(cachePath, 'utf8');
            return data.split(/[\n\r]/g);
        }
    }
    /**
     * Noop for exec
     * @method SyncIgnore#fakeExec
     * @return {Array<string>} Results
     */
    fakeExec() {
        const cachePath = path.join(this._cwd, SyncIgnore.FILENAME);
        fs.writeFileSync(cachePath, '');
        return [];
    }
    /**
     * Execute full search of folder tree, with the root being the specified
     * path.
     * @function SyncIgnore#_search
     * @private
     * @param  {string} packagePath root path
     * @return {SearchResults} results The results to update
     */
    _search(packagePath, results) {
        const packageFile = 'package.json';
        let pkg;
        try {
            pkg = require(path.join(packagePath, packageFile));
        }
        catch (e) {
            return;
        }
        const dependencies = []
            .concat(Object.keys(pkg.dependencies || []))
            .concat(Object.keys(pkg.optionalDependencies || []));
        for (let i = 0; i < dependencies.length; i++) {
            const name = dependencies[i];
            try {
                const modulePath = resolve.sync(path.join(name, 'package.json'), { basedir: packagePath });
                const dir = path.dirname(modulePath);
                results.appendProd(dir);
                // For any production modules that have a .syncignore file, 
                // we should apply that to the .syncignore cache
                results.appendFile(dir);
                // Do a search within this module and pass along the results
                this._search(dir, results);
            }
            catch (e) {
                // do nothing
            }
        }
        // Process the remaining node_modules
        // if they aren't in production, we should remove them
        this._searchRemainder(packagePath, results);
    }
    /**
     * Get the rest of the node_modules and add everything else
     * the results should consider anything else that isn't
     * already defined as a production dependency to be dev and ignored.
     * @function SyncIgnore#_searchRemainder
     * @private
     * @param {string} packagePath root path
     * @param {SearchResults} results The results to update
     */
    _searchRemainder(packagePath, results) {
        const dir = path.join(packagePath, 'node_modules');
        // Ignore missing node_modules folder
        if (!fs.existsSync(dir)) {
            return;
        }
        const folders = fs.readdirSync(dir);
        // Create a list of folders without namespaces
        const list = folders.filter(folder => {
            return folder[0] !== '@';
        });
        // Get all the folders within the individual 
        // namespace folders
        let namespacesList = [];
        const namespaces = folders.filter(folder => {
            return folder[0] === '@';
        });
        //  Get all the folders
        namespaces.forEach(ns => {
            const nsDir = path.join(packagePath, 'node_modules', ns);
            namespacesList = namespacesList.concat(
            // Prepend all items with the namesapce
            fs.readdirSync(nsDir).map(name => {
                return path.join(ns, name);
            }));
        });
        results.appendAll([]
            .concat(namespacesList)
            .concat(list), dir);
    }
}
exports.default = SyncIgnore;

},{"./SearchResults":1,"chalk":undefined,"fs":undefined,"minimist":undefined,"path":undefined,"resolve":undefined}],3:[function(require,module,exports){
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var SyncIgnore_1 = require("./SyncIgnore");
exports.SyncIgnore = SyncIgnore_1.default;

},{"./SyncIgnore":2}]},{},[3])(3)
});

//# sourceMappingURL=jibo-syncignore.js.map
